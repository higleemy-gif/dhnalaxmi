/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Property photos and the logo are stored locally under /public, so no
  // remote image hosts need to be allow-listed.
};

export default nextConfig;
